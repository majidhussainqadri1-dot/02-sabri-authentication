# R320 — Final Adversarial Review, Correction and Retest Evidence

Review baseline: `fe66788cb6c12e966bb198b361347cb74ec490b0`.

The complete R320 review was finished before product correction and its defect ledger was frozen first in `review-evidence/R320-REVIEW-FROZEN.md` at commit `59170cd0bd1b6363b7f3df0c4465ba9acab98094`.

## Frozen defects corrected

1. Material R311–R319 hardening still identified itself as runtime 1.2.1 / DB 1.2.0 / passkey schema 1.0.0.
2. Storage readiness proved required columns but not security-critical indexes and uniqueness constraints.
3. Permanent release and documentation CI remained tied to the old 1.2.1/R301–R310 truth and omitted the current regressions.
4. Real File 00 integration CI did not identify 1.2.2/DB 1.2.1/passkey 1.0.1 or rehearse legacy passkey-column reconciliation on MariaDB.
5. Release-facing documentation/SBOM and historical root review-report labeling were stale.
6. Temporary write-capable correction workflows and historical correction payloads remained in the source candidate.
7. Permanent review gates did not prevent correction machinery from returning.
8. File 00 taxonomy/provider-vocabulary harmonization remains an external File 00 owner-side release blocker; File 02 deliberately performs no lossy local remap.

## Product correction

R320 correction/retest workflow run `31814352841` completed successfully after applying the already-frozen ledger. The product/source correction commit is `448ac69739d7c18dea26a352336590c9f3d99b54`.

The corrected source identity is:

- Runtime: `1.2.2`
- File 02 DB identity: `1.2.1`
- Passkey schema identity: `1.0.1`
- Passkey assurance contract: `1.0.0` (unchanged)

Material readiness now proves required columns and named security-critical indexes/uniqueness constraints. The passkey migration reconciles legacy `credential_hash` / `credential_cipher` rows into canonical `credential_lookup_hash` / `credential_id_ciphertext` before successful schema readiness is accepted.

During the successful correction run, PHP lint and the cumulative source suite through `tests/r320-final-release-regression.php` passed before the corrected product commit was pushed.

## Permanent workflow-control cleanup

Because the GitHub Actions token intentionally lacked workflow-file mutation permission, workflow-control edits were applied separately through the authenticated GitHub connector after the product commit:

- release-integrity gate aligned with runtime `1.2.2`, DB `1.2.1`, passkey schema `1.0.1`, dynamic R31x/R32x regressions and deterministic packaging;
- canonical storage/docs gate aligned with the current review line and material index/schema truth;
- exact File 00 integration gate aligned with File 02 1.2.2 and a real MariaDB legacy-passkey upgrade rehearsal;
- review-branch integrity gate now rejects installable archives, temporary correction workflows, historical correction payloads and a surviving review apply-script;
- obsolete R310 and completed R311–R320 write-capable correction workflows were deleted.

Workflow-control commits in this cleanup sequence include `bcf422469d7251484fb3e09d120b5a8ab687370d`, `7533776f1c7eb1e92dfa63b5531140243fd959e0`, `0d3ee53f533828f7512446d26dedbee45629d9ae`, `ef7bebfd812fa3e67990c6f107d536dad460667a`, `16a6242a6dceab97fbfa5de9bf03913d09760150`, `091b8a3251737918315ac3aee7e5b61bc413047f`, and `6e8755a327d87f6b7daac692fd516e22a4fa0af3`.

## Evidence boundary

This file proves repository/source review and correction only. It does not prove that this branch is merged to `main`, packaged/deployed on staging or live, or that any deployed database has migrated to these versions. Live/staging evidence must remain separate.
